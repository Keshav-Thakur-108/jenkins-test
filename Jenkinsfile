pipeline {
    agent any

    options {
        skipDefaultCheckout()
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // Your build steps go here
            }
        }
    }
}