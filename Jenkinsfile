pipeline {
    agent any

    options {
        skipDefaultCheckout true
    }

    stages {
        stage('Build') {
            steps {
                echo 'hello world'
            }
        }
    }
}