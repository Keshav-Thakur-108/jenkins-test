pipeline {
    agent {
        label 'agent1'
    }

    options {
        skipDefaultCheckout()
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // Your build steps go here
            }
        }
    }
}